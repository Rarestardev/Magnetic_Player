<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="@dimen/radius_size"
    app:cardBackgroundColor="?colorPrimary">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <androidx.appcompat.widget.AppCompatEditText
            android:id="@+id/add_playlist_edittext"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginTop="@dimen/margin_between_elements"
            android:background="?colorAccent"
            android:gravity="start|center_vertical"
            android:hint="@string/play_list_name"
            android:inputType="text"
            android:layoutDirection="locale"
            android:maxLength="25"
            android:maxLines="1"
            android:padding="4dp"
            android:textAlignment="gravity"
            android:textColor="?android:textColor"
            android:textColorHint="@color/mid_white_light_mode"
            android:textStyle="bold"
            android:theme="@style/ThemeOverlay.Material3.TextInputEditText"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_save_new_playlist"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_between_elements"
            android:backgroundTint="?colorSecondary"
            android:clickable="true"
            android:text="@string/save_new_playlist"
            android:textColor="@color/white"
            android:textSize="@dimen/text_size_body1"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/add_playlist_edittext" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>